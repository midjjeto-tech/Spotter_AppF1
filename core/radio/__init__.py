"""core/radio — доменная модель радиообмена (событие → речь).

Граница ответственности. `core/commentary_events.py` — это ВХОД конвейера
(телеметрия → canonical event): он нормализует драфт, считает важность и
раздаёт событие в очередь и в RaceFeed. `core/radio` — ВЫХОД: превращает уже
отобранное событие вместе с готовым текстом в `RadioMessage`, который несёт
канал, срочность, срок жизни, политику прерывания, ключи ситуации и
UI-представление.

Разделение намеренное: `CommentaryEvent` не должен знать про озвучку, а
`RadioMessage` — про парсинг пакетов. См.
docs/superpowers/specs/2026-07-29-f1-manager-radio-redesign.md.

Модули:
  policy.py      — канал / срочность / TTL / политика прерывания / категории
  situations.py  — situation_id (личность ситуации) и dedupe_key (высказывание)
  message.py     — RadioMessage и его состояния
"""
