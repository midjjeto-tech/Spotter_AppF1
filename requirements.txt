# -*- coding: utf-8 -*-
bottle>=0.13
# Пин на ==, а не >=: core/overlay_window.py::sync_once() зависит от точного
# поведения pywebview.platforms.edgechromium.EdgeChrome.on_navigation_start
# (форсит form.Show()/Activate() для transparent-окон в обход нашего
# контроллера — sync_once() специально пере-применяет layered-стиль и
# скрытие каждый тик, чтобы это компенсировать). Поднимать пин осознанно,
# сверяя это поведение в новой версии, а не молча увеличивать нижний порог.
pywebview==6.2.1
psutil>=5.9
aiohttp>=3.9
pyttsx3>=2.90
# Приглушение звука игры на время реплики (core/audio_ducking.py).
# Тонкая обёртка над comtypes, который уже нужен SAPI5-драйверу pyttsx3.
pycaw>=20240210
piper-tts>=1.4.0
onnxruntime>=1.20.0
numpy>=1.24
# pandas убран 2026-08-08. Он был жёсткой зависимостью движка ровно потому, что
# core.engine -> analytics.loader -> analytics.openf1_loader импортировал его на
# уровне модуля. openf1_loader удалён вместе с источником, и pandas не
# импортирует больше ни один модуль проекта.
# fastf1 удалён 2026-08-08 вместе с Real-F1 Benchmark: код пакета под MIT, но
# данные приходят из неофициального источника Formula 1 без коммерческого
# разрешения (см. NOTICE). Эталон темпа теперь берётся из телеметрии самой игры
# (core/f1_benchmark.py), эталон разбора — из своего архива (analytics/loader.py).
sounddevice>=0.4
soundfile>=0.12

# --- RAG (commentator/rag.py) — ЗАВИСИМОСТЬ УБРАНА 2026-08-09 ---
# Было: sentence-transformers>=2.7 (тянет torch + transformers).
#
# В собранном приложении RAG не работал НИ РАЗУ и работать не мог: `torch` и
# `transformers` стоят в excludes SpotterApp.spec, поэтому в EXE импорт всегда
# падал и F1RAG молча уходил в disabled. То есть зависимость на несколько
# гигабайт скачивал каждый, кто ставит из исходников, ради фичи, которая живёт
# только в дереве разработки.
#
# Код и база (commentator/knowledge_base/f1_facts.json, 62 факта) оставлены:
# они деградируют чисто (brain.py держит rag=None) и ничего не весят. Хотите
# оживить фичу в релизе — не возвращайте sentence-transformers: под 62 факта
# эмбеддинг-модель на ~470 МБ избыточна, дешевле переписать поиск на ключевые
# слова, и тогда он поедет в EXE вообще без зависимостей.

# --- Yandex Cloud (gRPC client for SpeechKit v3) ---
yandexcloud>=0.398.0       # gRPC client for SpeechKit v3 (Synthesizer.UtteranceSynthesis)

# --- Sber GigaChat (альтернативный «мозг», config.LLM_PROVIDER="gigachat") ---
# Официальный SDK: сам делает OAuth (30-мин токен + рефреш) и TLS-сертификат
# Минцифры. Голос при этом остаётся Yandex SpeechKit.
gigachat>=0.2.0

# --- iRacing telemetry source (core/iracing_telemetry.py) ---
# В F1-релиз 0.2 НЕ входит: README честно фиксирует незавершённые фазы адаптера,
# а единственный pyirsdk 1.1.7 на PyPI не собирается в изолированном Python 3.12
# окружении (setup.py импортирует yaml до объявления build-зависимостей).
# core/iracing_telemetry.py штатно деградирует до iracing_no_lib. Возвращать
# пакет в release lock только вместе с завершением адаптера и воспроизводимым
# wheel/исправленным upstream-пакетом, а не через небезопасный --no-build-isolation.

# --- Только для разработки/тестов (не нужны в рантайме) ---
# pytest>=8.0
#
# anthropic больше не используется (заменён на Yandex Foundation Models).
# Yandex GPT/SpeechKit работают по REST через aiohttp — отдельный SDK не нужен.

mss>=9.0  # RaceFeed hero-post screenshots (optional; degrades if absent)
