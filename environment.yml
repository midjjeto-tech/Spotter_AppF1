name: spotter
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - bottle>=0.13
      - pywebview>=5.0
      - psutil>=5.9
      - pyttsx3>=2.90
      - qwen-tts
      - sounddevice>=0.4
      - soundfile>=0.12
      - torch>=2.1
      - torchvision
      - torchaudio
