# Транслитерация латиницы для пилотов вне статического словаря — дизайн

Дата: 2026-07-09
Статус: утверждён пользователем (диалог 2026-07-09).

## Проблема

Бэклог-пункт из «Известных gotchas»: «Латиница от Jolpica для пилотов ВНЕ
статического словаря не транслитерируется. Русский TTS озвучит её мусором.»
Расследование нашло два места с одной и той же проблемой:

- `core/f1_metadata.py::enrich_driver` — когда номер машины не в
  `F1_2025_BY_NUMBER`/`F1_2026_BY_NUMBER` (новичок/будущий сезон), имя
  резолвится через Jolpica (`self._by_number`/`self._drivers`), откуда оно
  приходит на латинице (`givenName`+`familyName` из Ergast API) без какой-либо
  транслитерации — эта латиница течёт прямо в `result["name"]`, оттуда в UI и
  в Yandex TTS.
- `core/f1_benchmark.py::_ru_driver` — точечный словарь `_LATIN_TO_RU` (24
  записи) для контекста бенчмарка; для имён вне словаря —
  `return _LATIN_TO_RU.get(latin, latin)` — та же латиница как есть.

## Согласованный объём

- **Правило-базовый алгоритм, без новых зависимостей.** Точная фонетическая
  транслитерация (словарь произношений/ML) — избыточно для редкого edge-кейса.
  Набор диграф/буквенных правил практической транслитерации.
- **Явно принятое ограничение, не скрываем:** правила калиброваны на
  английскую орфографию — фамилии не-английского происхождения (голландские,
  французские, японские и т.п.) могут транслитерироваться неверно. Проверка
  на уже известных именах из статических словарей показала: около половины
  совпадает с ручными записями (Bearman→Бирман через "ea"→"и", Norris→Норрис,
  Piastri→Пиастри, Tsunoda→Цунода через "ts"→"ц"), часть — не совпадает
  (Verstappen — голландское произношение "V"→"ф", буквенные правила дадут
  «Верстаппен»; Leclerc — французская тихая "c"; Hamilton/Russell —
  неоднозначные английские гласные). Алгоритм — подстраховка «лучше так, чем
  сырая латиница», НЕ замена ручной курации точных случаев в статических
  словарях/`_LATIN_TO_RU`.
- **Один общий транслитератор для обоих мест** (не два независимых решения
  одной задачи).
- **Не трогаем** `core/ru_names.py::decline()` (уже безопасно фолбэкается на
  несклоняемую форму для неизвестных фамилий) и `core/pronunciation.py`
  (транслитерированные имена не получают ручную разметку ударения — тот же
  статус, что и у любого сейчас неизвестного имени).

## Дизайн

### 1. `core/transliterate.py` (новый модуль)

```python
"""
core/transliterate.py
======================
Правило-базовая практическая транслитерация латиницы в кириллицу — подстраховка
для имён ВНЕ статических словарей (core/f1_metadata.py::F1_2025_BY_NUMBER/
F1_2026_BY_NUMBER, core/f1_benchmark.py::_LATIN_TO_RU). Русский Yandex TTS
озвучивает сырую латиницу мусором — транслитерированная кириллица звучит
лучше, даже когда неточна.

ВАЖНО: правила калиброваны на английскую орфографию. Фамилии не-английского
происхождения могут транслитерироваться неверно (пример: "Verstappen" —
голландское произношение с "V" как "ф" — этот модуль даст "Верстаппен", не
"Ферстаппен", как в статическом словаре). Это ПРИНЯТОЕ ограничение, не баг —
для точных случаев имя фиксируется вручную в соответствующем словаре, этот
модуль — только фолбэк для того, чего там ещё нет.
"""
from __future__ import annotations

import re

_LATIN_RE = re.compile(r"^[A-Za-z\s\-'.]+$")

# Диграфы — длиннее совпадение приоритетнее однобуквенного. Порядок в списке
# ниже важен только для читаемости, сопоставление всегда идёт по длине.
_DIGRAPHS: dict[str, str] = {
    "th": "т", "ch": "ч", "sh": "ш", "ph": "ф", "ck": "к", "qu": "кв",
    "wh": "в", "ea": "и", "ee": "и", "oo": "у", "ai": "ай", "ay": "ай",
    "ey": "ей", "oy": "ой", "ow": "оу", "ou": "ау", "aw": "оу", "ew": "ью",
    "dj": "дж", "ts": "ц", "kh": "х", "zh": "ж", "gh": "г",
}

_LETTERS: dict[str, str] = {
    "a": "а", "b": "б", "c": "к", "d": "д", "e": "е", "f": "ф", "g": "г",
    "h": "х", "i": "и", "j": "дж", "k": "к", "l": "л", "m": "м", "n": "н",
    "o": "о", "p": "п", "q": "к", "r": "р", "s": "с", "t": "т", "u": "у",
    "v": "в", "w": "в", "x": "кс", "y": "и", "z": "з",
}

_MAX_DIGRAPH_LEN = max(len(k) for k in _DIGRAPHS)


def _transliterate_word(word: str) -> str:
    lower = word.lower()
    out: list[str] = []
    i = 0
    while i < len(lower):
        matched = False
        for length in range(_MAX_DIGRAPH_LEN, 1, -1):
            chunk = lower[i:i + length]
            if chunk in _DIGRAPHS:
                out.append(_DIGRAPHS[chunk])
                i += length
                matched = True
                break
        if matched:
            continue
        ch = lower[i]
        out.append(_LETTERS.get(ch, ch))   # не-буква (дефис, апостроф) — как есть
        i += 1
    result = "".join(out)
    return result[:1].upper() + result[1:] if result else result


def is_latin(text: str) -> bool:
    """True, если строка целиком латиница (плюс пробелы/дефис/апостроф) —
    значит транслитерация имеет смысл; кириллицу/смешанный текст не трогаем."""
    return bool(text) and bool(_LATIN_RE.match(text))


def to_cyrillic(latin: str) -> str:
    """Транслитерировать латинское имя (одно слово или полное имя из
    нескольких слов — каждое обрабатывается независимо, разделители
    сохраняются) в кириллицу. Не проверяет is_latin() сама — вызывающий код
    решает, когда транслитерация нужна."""
    if not latin:
        return latin
    return " ".join(_transliterate_word(w) if w else w for w in latin.split(" "))
```

### 2. `core/f1_metadata.py::enrich_driver` — новый финальный слой

Перед `return result` (после всех существующих 4 шагов), если имя всё ещё
латиница:

```python
        # 5. Транслитерация — последний фолбэк, если имя до сих пор латиница
        #    (Jolpica отдала непереведённое имя, номера нет в статическом
        #    словаре). Лучше приблизительная кириллица, чем сырая латиница в
        #    Yandex TTS. Точные случаи — в F1_2025_BY_NUMBER/F1_2026_BY_NUMBER,
        #    это не замена им (см. core/transliterate.py).
        if result.get("name") and transliterate.is_latin(result["name"]):
            result["name"] = transliterate.to_cyrillic(result["name"])

        return result
```

Импорт `from core import transliterate` добавляется в блок импортов файла.

### 3. `core/f1_benchmark.py::_ru_driver` — фолбэк вместо латиницы как есть

```python
def _ru_driver(latin: str | None) -> str:
    if not latin:
        return ""
    return _LATIN_TO_RU.get(latin, transliterate.to_cyrillic(latin))
```

Импорт `from core import transliterate` добавляется в блок импортов файла.

## Отказоустойчивость

Чистая, без сети/I/O функция над строкой — не может бросить исключение на
любом входе (`to_cyrillic("")` → `""`, не-буквенные символы копируются как
есть). `is_latin()` — простой regex-матч, безопасен на `None`/пустой строке
(явная проверка `bool(text)` до матча).

## Тестирование

- `tests/test_transliterate.py` (новый):
  - «Известные совпадения» — `to_cyrillic("Bearman") == "Бирман"`,
    `to_cyrillic("Norris") == "Норрис"`, `to_cyrillic("Piastri") == "Пиастри"`,
    `to_cyrillic("Tsunoda") == "Цунода"`, `to_cyrillic("Alonso") == "Алонсо"`,
    `to_cyrillic("Sainz") == "Сайнс"`, `to_cyrillic("Antonelli") ==
    "Антонелли"`, `to_cyrillic("Colapinto") == "Колапинто"`,
    `to_cyrillic("Gasly") == "Гасли"`, `to_cyrillic("Ocon") == "Окон"`,
    `to_cyrillic("Stroll") == "Стролл"`, `to_cyrillic("Albon") == "Албон"`,
    `to_cyrillic("Lawson") == "Лоусон"`, `to_cyrillic("Hadjar") == "Хаджар"`.
  - «Принятое несовпадение» — `to_cyrillic("Verstappen") == "Верстаппен"`
    (НЕ `"Ферстаппен"`) с комментарием, объясняющим почему (голландское
    произношение) и что это ожидаемо, не баг.
  - `is_latin()` — True для латиницы, False для кириллицы/смешанного/пустой
    строки/`None`.
  - Полное имя из нескольких слов транслитерируется по словам:
    `to_cyrillic("Oliver Bearman") == "Оливер Бирман"`.
- `tests/test_f1_metadata.py` (расширение, если файл существует, иначе
  проверить существующий тестовый файл для `F1Metadata`) — `enrich_driver`
  транслитерирует имя, пришедшее из Jolpica для номера вне статического
  словаря.
- `tests/test_f1_benchmark.py` — `_ru_driver` для латинского имени вне
  `_LATIN_TO_RU` возвращает транслитерацию, не сырую латиницу.
- Полный `pytest` — без регрессий.
